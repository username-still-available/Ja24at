{
  "name": "ja24.at---zuzana-jancova",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx tailwindcss -i ./src/css/input.css -o ./_site/css/style.css",
    "watch:css": "npx tailwindcss -i ./src/css/input.css -o ./_site/css/style.css --watch",
    "build:11ty": "eleventy",
    "build": "npm run build:css && npm run build:11ty",
    "clean": "rimraf _site",
    "fullbuild": "npm run clean && npm run build",
    "verify:js": "echo \"Verifying JavaScript files are correctly placed in build output...\"",
    "postbuild": "npm run verify:js",
    "watch:11ty": "eleventy --serve",
    "start": "npm-run-all --parallel watch:*",
    "netlify": "npm install @11ty/eleventy-img sharp && npm run clean && npx tailwindcss -i ./src/css/input.css -o ./src/css/style-production.css && cp ./src/css/style-production.css ./src/css/style.css && eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^3.1.8",
    "@netlify/plugin-lighthouse": "^6.0.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.0.1",
    "tailwindcss": "^3.3.3"
  }
}
